{
  "name": "crisp-mcp-http-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild api/mcp.ts --bundle --platform=neutral --format=esm --outfile=dist/api/mcp.mjs",
    "start": "vercel dev"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.1.0",
    "undici": "^6.19.8"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.6.3"
  }
}
